@AGENTS.md

For all Java code and tests, follow
`.agents/skills/seedu-java-coding-standard/SKILL.md`, including during merge conflict resolution.
Before proposing or creating any commit, follow `.agents/skills/seedu-git-standard/SKILL.md`.
Read these project skills rather than relying on remembered conventions.

Follow the UI testing workflow in `AGENTS.md`: refresh `test/ui-test-plan.md` if needed and run the `test-ui` skill after code changes.

Follow the JUnit policy in `AGENTS.md`: focus on roughly the 50% highest-value methods and
review/update the relevant JUnit tests after every code change to maintain that target.
